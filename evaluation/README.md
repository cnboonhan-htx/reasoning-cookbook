# Evaluation

Automated evaluation of trained checkpoints. Includes benchmark definitions (SimWorld scenarios as eval tasks), metrics computation (task success rate, reasoning quality, physical plausibility), and scripts that take a checkpoint, run eval in SimWorld, and output a report.
