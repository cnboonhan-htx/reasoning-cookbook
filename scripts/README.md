# Scripts

Cluster job submission scripts (SLURM or equivalent), Dockerfiles/container definitions for reproducible training environments, and a pipeline orchestrator (Makefile or simple Python CLI) that chains: data prep -> training -> checkpoint management.
